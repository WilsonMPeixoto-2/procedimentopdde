name: CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    name: Typecheck, Lint, Test, Build & E2E
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Refresh lockfile for dependency modernization
        if: github.event_name == 'pull_request' && github.head_ref == 'chore/dependency-refresh-2026-08'
        shell: bash
        run: |
          rm -f package-lock.json
          npm install --package-lock-only

      - name: Install dependencies
        run: npm ci

      - name: Persist refreshed lockfile
        if: github.event_name == 'pull_request' && github.head_ref == 'chore/dependency-refresh-2026-08'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          if git diff --cached --quiet; then
            echo "package-lock.json already current"
          else
            git commit -m "chore(deps): regenerate canonical npm lockfile [skip ci]"
            git push origin HEAD:${{ github.head_ref }}
          fi

      - name: Dependency audit
        run: npm audit --audit-level=high

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: E2E and accessibility
        run: npm run test:e2e
